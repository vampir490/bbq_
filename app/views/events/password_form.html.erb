<h1>Access only with PIN</h1>

<%= form_tag('', method: 'post') do |f| %>
  <div class="form-group">
    <%= label 'PIN', :pincode %>
    <%= text_field_tag :pincode, params[:pincode], class: 'form-control' %>
  </div>

  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>

<%= link_to 'To the main page', events_path %>